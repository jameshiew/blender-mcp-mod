Control Blender through its MCP add-on. Start with get_addon_status, then inspect the scene before editing and use exact names. Check get_blender_api_info before writing unfamiliar Python. Batch related edits into one execute_blender_code call and print concise results. Verify visible changes with get_viewport_screenshot. Paged results report has_more and next_offset; details_omitted marks capped details. File loads, undo/redo, and checkpoint restores clear execution namespaces. Credit imported assets as their attribution requires.
