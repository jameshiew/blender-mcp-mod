[project]
name = "blender-mcp-addon-tests"
version = "1.9.1"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
python_files = ["test_*.py"]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "requests>=2.34.2",
]

[tool.uv]
package = false
