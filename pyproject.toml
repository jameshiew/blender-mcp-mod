[project]
name = "blender-mcp-addon-tests"
version = "2.0.0+mod"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
python_files = ["test_*.py"]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "ruff>=0.16.6",
]

[tool.uv]
package = false
